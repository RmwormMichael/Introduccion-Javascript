// Objeto Math

let resultado;

resultado = Math.PI;
resultado = Math.round(2.5);
resultado = Math.ceil(2.1);// ceil redondea siempre hacia arriba
resultado = Math.floor(2.9);// ceil redondea siempre hacia abajo
resultado = Math.sqrt(144);// Raiz cuadrada
resultado = Math.abs(-200);// Convierte numero negativo en positivo
resultado = Math.min(3, 5, 1, 8, 2, 10);// muestra el minimo numero
resultado = Math.min(3, 5, 1, 8, 2, 10);// muestra el maximo numero
resultado = Math.random();// muestra numero aleatorio
resultado = Math.floor( Math.random() * 30);// muestra numero aleatorio

console.log(resultado); 